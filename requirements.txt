# Core dependencies
numpy>=1.21.0
sounddevice>=0.4.0
soundfile>=0.10.0
PyYAML>=6.0
requests>=2.25.0
psutil>=5.8.0
flask>=2.0.0
flask-socketio>=5.0.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
PyPDF2>=3.0.0
python-docx>=0.8.11

# Security dependencies
PyJWT>=2.0.0
cryptography>=3.4.0

# TTS dependencies (optional - system has fallback to pyttsx3)
# Piper TTS (recommended - neural voices, high quality)
piper-tts>=1.3.0
# Kokoro TTS (advanced interruptible - framework ready)
# kokoro>=0.7.0; python_version < "3.13"

# LiveKit agents for voice activity detection
livekit-agents[turn-detector]>=1.2.0

# WebSocket communication for web dashboard
python-socketio>=5.0.0
flask-socketio>=5.0.0
flask-cors>=4.0.0
websockets>=12.0

# Development dependencies (uncomment for development)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0
# mypy>=0.950
